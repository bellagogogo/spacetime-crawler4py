1. install bs4
2. run the scraper
3. Check out worker.py, uses multi threads here

TODO LIST:

4. token_hash = hash(tuple(tokenized)) -> Maybe Use a better hash method here
5. make stop words better (put it in a seperate file)
6. Need to handle 304 url redirection
7. Check if the traps are handle properly